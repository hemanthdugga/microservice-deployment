pipeline {
    agent any 

    environment {
        GIT_REPO_NAME = "micro-service"
        GIT_EMAIL = "gsharma1298@gmail.com"
        GIT_USER_NAME = "gsharma1298"
        IMAGE_NAME = "adservice"
        REPO_URL = "623577189957.dkr.ecr.ap-south-1.amazonaws.com/adservice"
        YAML_FILE = "adservice.yaml"
        GIT_TOKAN = credentials('git-tokan')
    }

    stages {
        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout from Git') {
            steps {
                git branch: 'master', url: 'https://github.com/gsharma1298/micro-service.git'
            }
        }

        stage("Docker Image Build") {
            steps {
                script {
                    dir('src/adservice') {
                        sh 'docker system prune -f'
                        sh 'docker container prune -f'
                        sh 'docker build -t adservice .'
                    }
                }
            }
        }

        stage("ECR Image Pushing") {
            steps {
                script {
                    sh '''
                aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 623577189957.dkr.ecr.ap-south-1.amazonaws.com    
		docker tag adservice:latest 623577189957.dkr.ecr.ap-south-1.amazonaws.com/adservice:${BUILD_NUMBER}
                    docker push 623577189957.dkr.ecr.ap-south-1.amazonaws.com/adservice:${BUILD_NUMBER}
                    '''
                }
            }
        }

    stage('Update Deployment file') {
            steps {
                dir('kubernetes-files') {
                    withCredentials([string(credentialsId: 'git-tokan', variable: 'git_token')]) {
                        sh '''
                            git config user.email "${GIT_EMAIL}"
                            git config user.name "${GIT_USER_NAME}"
                            echo $BUILD_NUMBER

                            sed -i "s#image:.*#image: ${REPO_URL}:$BUILD_NUMBER#g" ${YAML_FILE}
                            git add .
                            git commit -m "Update ${IMAGE_NAME} Image to version ${BUILD_NUMBER}"
                            git push https://${git_token}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:master
                        '''
                    }
                }
            }
        }
    }
}
